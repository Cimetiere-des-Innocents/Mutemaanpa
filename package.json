{
  "devDependencies": {
    "husky": "^8.0.0"
  },
  "scripts": {
    "prepare": "husky install && cd hooks && npm install && npm run build && node ./dist/post-merge.js",
    "doc": "mdbook build docs/mutemaanpa-dev-book && mdbook build docs/mutemaanpa-background-book && mdbook build docs/mutemaanpa-story-book"
  }
}