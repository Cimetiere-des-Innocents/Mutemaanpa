{
  "devDependencies": {
    "husky": "^8.0.0"
  },
  "scripts": {
    "prepare": "husky install && cd hooks && npm install && npm run build && node ./dist/post-merge.js",
    "doc": "mdbook build mutemaanpa-dev-book && mdbook build mutemaanpa-background-book && mdbook build mutemaanpa-story-book"
  }
}